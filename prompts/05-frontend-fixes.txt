[Prompt sent from agent maestro to agent frontend.]

Two frontend fixes needed. Keep 100% coverage (add tests for new behavior), contract still frozen, accumulator model unchanged.

1) KEYBOARD SUPPORT — currently only mouse clicks work; keyboard does nothing. Add full keyboard input, routed through the SAME handlers as the buttons (no duplicate logic). Mapping: digits 0-9 and '.' -> digit entry; '+' '-' -> add/subtract; '*' or 'x' -> multiply; '/' -> divide; '^' -> power; '%' -> percentage; 'r' -> sqrt; Enter or '=' -> equals; Escape or 'c'/'C' -> AC/clear; Backspace -> delete last char if you support it, else ignore. Use a window/document keydown listener in a useEffect (cleaned up on unmount); call preventDefault where the key would otherwise do something (e.g. '/'). Optional but nice: brief visual active-state on the matching button when its key is pressed. Add Vitest+RTL tests: each key triggers the right action, Enter=equals fires a backend call, Escape clears, listener is removed on unmount.

2) REMOVE COPY — delete the user-facing texts 'backend-authoritative' and 'left-to-right, one backend call per step' (and close paraphrases) from the FRONTEND only: any on-screen UI text AND frontend/README.md. Do NOT touch docs/adr/ or root docs — those architecture records stay. Just strip it from what a user/reader of the frontend sees.

Reply CONCISE: files changed, keyboard mapping implemented, new test count, coverage % after.