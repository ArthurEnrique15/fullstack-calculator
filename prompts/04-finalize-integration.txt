[Prompt sent from agent maestro to agent integration-and-docs.]

Both layers have LANDED — finalize now. You are wired to Bedrock and Prism on the canvas (maestri list). Backend facts from Bedrock: run 'cd backend && go run .' (env PORT default 8080, ALLOWED_ORIGIN default *); tests+coverage 'go test -race -coverpkg=./calculator,./handler -coverprofile=coverage.out ./... && go tool cover -html=coverage.out -o coverage.html' = 100%; backend Dockerfile is multi-stage, exposes :8080; packages are calculator/ + handler/ + main.go at backend root (module path in go.mod). Frontend facts from Prism: 'cd frontend && npm install && npm run dev' (:5173), 'npm test' (57 tests), 'npm run test:coverage' = 100% lines/stmts/funcs (99.1% branch, threshold 95); frontend Dockerfile multi-stage node->nginx, nginx.conf already serves SPA + proxies /api -> backend:8080. NOTE backend also uses :8080 internally — in docker-compose keep backend UNPUBLISHED to host (expose only), publish only the frontend to a host port. If anything is genuinely ambiguous, use grill-with-docs; otherwise ask Bedrock/Prism directly via maestri ask. FINALIZE: run 'docker compose up --build', run ./scripts/smoke.sh against the published frontend URL, verify both coverage reports render, swap real backend cmds into README, drop the DRAFT banner. Do NOT write to prompts/ (rule changed — manager owns it). Reply CONCISE: compose up result, smoke test pass/fail counts, and anything broken.