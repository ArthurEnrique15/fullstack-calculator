[Prompt sent from agent maestro to agent frontend.]

Code-review fixes for frontend/. Keep 100% coverage (add tests for new behavior), contract frozen, accumulator model unchanged. Apply: (1) THROW [manager decided] — api.ts:~30: when a binary op is called with b === undefined, throw TypeError("operand b required for binary op") instead of b ?? 0. Add a test. (2) api.ts:~4 — split code lists: SERVER_CODES = the allow-list straight from docs/API-CONTRACT.md; CLIENT_CODES = NETWORK_ERROR, UNKNOWN. toErrorCode must narrow to SERVER_CODES only; any other/unknown backend code -> UNKNOWN. Add a test that a bogus server code maps to UNKNOWN. (3) useCalculator.ts (~41/52/63) — callbacks currently depend on the whole state object, so the api ref changes every render and useKeyboardControls re-attaches the window keydown listener on every keystroke. Fix: hold state in a useRef refreshed each render (or use functional dispatch) and/or useMemo the returned api, drop state from deps so the listener attaches once. Add/adjust a test asserting the keydown listener is not re-attached on each keystroke (e.g. addEventListener spy called once). (4) ERROR-STATE UI [manager decided] — while state.error !== null, DISABLE every button except AC/clear. Add a test. (5) accumulator.ts:~53 — drop the dead .replace("-",""); match the plain .length style used by DECIMAL; add a short invariant comment (user-typed display is unsigned). (6) accumulator.ts:~35 — inline return String(n). (7) Button.test.tsx:~15 — stop asserting Tailwind class fragments; expose a data-variant attribute on the button and assert that (or delete those brittle assertions if behavior is already covered). (8) useKeyboardControls.test.tsx:~44 — replace the double Promise.resolve micro-tick with await waitFor(...). Re-run npm run test:coverage, confirm 100% stmts/lines/funcs. Reply CONCISE: what changed, test count, coverage %.